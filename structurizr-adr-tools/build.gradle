version = '1.3.8'

dependencies {

    compile 'com.structurizr:structurizr-core:1.9.5'

    testCompile 'junit:junit:4.12'

}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
    test {
        java {
            srcDir 'test/unit'
        }
    }
}